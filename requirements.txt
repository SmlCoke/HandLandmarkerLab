# Runtime dependencies for Ubuntu 20.04, Python 3.8, TensorFlow 2.9 and CUDA 11.2.
# Install from the repository root after activating the target Conda environment.

# Core training stack. NumPy/protobuf/h5py are pinned to versions compatible
# with the legacy TensorFlow 2.9 ABI and serializer behavior.
tensorflow==2.9.0
keras==2.9.0
numpy==1.23.5
protobuf==3.19.6
h5py==3.7.0
flatbuffers==1.12

# Data and configuration.
PyYAML==6.0.1
Pillow==9.5.0
opencv-python-headless==4.8.1.78

# Terminal training progress.
tqdm==4.66.5

# ONNX export and CPU-side parity checks. CPU ONNX Runtime is deliberate: the
# training GPU remains owned by TensorFlow and export validation does not need
# another CUDA runtime binding.
onnx==1.12.0
tf2onnx==1.14.0
onnxruntime==1.13.1
